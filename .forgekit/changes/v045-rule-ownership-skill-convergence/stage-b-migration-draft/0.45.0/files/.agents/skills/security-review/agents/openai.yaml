interface:
  display_name: "Security Review"
  short_description: "Audit real security boundaries read-only"
  default_prompt: |-
    $security-review
    Review the specified security boundary and report severity, evidence level, impact, and verification gaps without modifying files.
