# 入口所有权与当前事实修复

状态：已完成。九份文档修复、独立审查、临时副本与实际普通升级均完成；实际项目为 0.47.0。新任务新开会话。

1. Problem / Goal：修复指定项目的整份定制入口、重复过时状态和升级衔接，使普通统一入口可完成 0.46 → 0.47，后续维护不再覆盖模板区。
2. Known Facts：根 AGENTS 无用户区且整份改写；三层入口、地图及冻结状态文档缓存旧身份，LIVE app-current 清单已有不同身份；现有 README 已采用现场引用；默认冲突为 AGENTS 一项。证据为目标项目对应文件与只读迁移计划。
3. Assumptions：磁盘 Program 身份不等于进程健康、成功部署或用户验收。本轮不推断新版本验收，也不修改生产。
4. Constraints：用户本轮要求修复，授权项目文档整理与 ForgeKit 升级。仅精确文件允许列表；不提交、推送、构建、部署、操作 DATA/Scheduler/source、运行服务或清理。保留全部历史验收证据、项目路径和授权约束。写前快照、输入哈希校验与失败恢复。
5. First-Principles Derivation：模板升级只拥有模板规则；项目用户区拥有额外边界和路由；易变运行身份引用 LIVE manifest/status，验收事实仍要求对应证据。三者不能共享一个可覆盖文本区域或把复制品当当前事实。
6. Surface Fix vs Root Fix：原文整份保留只能解决文本不丢失，不能证明事实仍有效。本轮不新增绕过开关、不放宽迁移引擎，而把项目入口规范化为准确旧模板基线＋经过整理的用户区，再沿正常版本链更新模板区。过时运行值改为现场引用，历史冻结文档明确历史/当前证据边界。
7. Minimal Verifiable Outcome：普通计划零冲突；副本普通 CLI apply 成功；模板区等于新模板、用户区保留、原始文件备份可还原；三层入口/地图不缓存 release/commit/scheduler；历史证据仍在；只读任务不被入口要求自动写回；现有文档契约检查与 current-docs 检查通过；新模板下一次修改的合成迁移仍保留用户区。
8. Recommended Next Step：准备文件差异和迁移候选，独立只读审查＋副本应用，再用已获授权的统一入口对指定项目应用，最终只做验证与最小写回收口。

实现材料在本机忽略目录 `user-rules/reviewed-upgrades/autumn-ownership-repair/`，不分发项目路径或内容。实际项目修改前审查该精确候选。若发现输入变化，重新规划，不覆盖并发编辑。

## 验证与收口

候选集合 SHA256：`f9d035a3038beb2ef919231971ec6ba99360f4e2bbaa8c87a155988743c50c19`。实际完成 111 个迁移动作并逐项核对哈希；普通统一入口再次返回 up-to-date、零动作。模板区匹配 0.47，用户规则保留，内层文档匹配审查候选。

既有文档契约 18 项中 17 通过；采集文案断言第 264 行在修改前、候选和实际项目同样失败，未修改其对应文档或测试，无新增失败。该已证实的无关失败不扩大修复范围。current-docs 检查通过；完整 ForgeKit 门禁 288 项、281 通过、7 跳过，125.050 秒，exit 0。

独立审查发现一次性辅助程序的回滚可覆盖并发编辑，已增加恢复前内容核对及恢复后字节验证，第三种内容保留并报告恢复缺口。九个失败点恢复、写前并发与回滚窗口并发保留验证通过；定向复核 pass。最终辅助程序 SHA256：`4bedd04ab387ddf594bb8365d860140c68a4d3ca0d174ac54a9acc722064266d`。

目标项目实际记录位于 `.forgekit/changes/forgekit-047-entry-repair/summary.md`，包含来源、决策、验证、独立审查、原文备份和升级结果路径。任务 TASK-047 / SRC-20260907-047 已收口。没有提交、推送、构建、部署或生产操作；当前生产验收仍需对应证据，不把本轮文档修复写成业务验收通过。
