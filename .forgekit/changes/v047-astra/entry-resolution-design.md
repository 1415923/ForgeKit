# 整份定制入口的显式合并

状态：实现完成；完整门禁、临时副本应用和独立审查通过。真实项目未执行升级。证据见 verification.md 与 review.md 的最新补充。

已确认：本次报告的入口为整份项目专用规则，涉及启动顺序、生产目录与授权边界；默认三方合并不具备判断这些规则语义兼容性的能力。首个差异在基线第 1 行，不表示只修改了标题。

本轮范围：新增显式 `--entry-resolutions`，仅处理 AGENTS.md / CLAUDE.md。JSON 绑定项目绝对路径、起始版本、迁移 ID、本地入口、传入模板和候选内容哈希。候选只能由新版入口加完整原文用户区组成，不能通过该入口删减项目规则。人工或独立审查评估两套规则的兼容性；哈希校验不代替语义审查。默认规则替换继续阻断。

接受标准：默认整份改写停止且无写入；显式候选在临时副本成功应用并保留全部原文；错误路径/版本/迁移/哈希、无效目标、候选删改、计划后材料变化与未使用条目均拒绝；统一入口透传计划与应用参数；审查记录可关联候选哈希。

权限：已授权修复 ForgeKit；真实业务项目仅只读预检。所有应用实验在临时副本进行。实际升级留给用户执行统一入口的确认步骤，不执行部署、构建业务系统、生产操作、提交或发布。
