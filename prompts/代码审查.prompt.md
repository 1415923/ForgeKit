# ForgeKit v0.45 兼容提示词：代码审查

> Deprecated compatibility prompt: v0.45-v0.46。首选直接调用项目内 Skill。

$code-review

请只读审查当前实现、diff、测试和证据，按 BLOCKER、MAJOR、NOTE 报告结果。证据不足时标记 `NEEDS_TEST`。finding 不授权自动修复，也不自动调用安全审查或发布检查。
