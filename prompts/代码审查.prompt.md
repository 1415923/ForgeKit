# ForgeKit v0.46 兼容提示词：代码审查

> Deprecated compatibility prompt for v0.46。首选直接调用项目内 Skill。

$code-review

请只读审查当前实现、diff、测试和证据，分别报告 `Impact Severity` 与 `Blocking`。`Blocking=YES` 必须给出唯一 C1-C4 PrimaryConsequence、FailurePath、Evidence 和 BlockedScope；高 Severity 或命令 nonzero 不自动产生 Blocking。证据不足时标记 `NEEDS_TEST`。finding 不授权自动修复，也不自动调用安全审查或发布检查。
