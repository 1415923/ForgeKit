# ForgeKit v0.46 兼容提示词：实现任务

> Deprecated compatibility prompt for v0.46。代码实施是直接任务，不强制依次调用全部 Skill。

请按当前用户授权和项目入口直接实施 `<任务>`，先确认可验证范围。仅在真实高影响风险时使用 `$large-change-planning`；仅在明确安全影响时使用 `$security-review`；需要独立审查时由风险、用户要求或阶段合同决定。不要自动 commit、push、tag、release 或 deploy。
