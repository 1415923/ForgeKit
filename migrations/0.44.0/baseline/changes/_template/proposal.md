Status: draft
Risk: medium
Created: YYYY-MM-DD
Owner: <name>
Reason: <short reason>

# 变更提案

## 问题

- 要解决什么问题？

## 目标

- 变更完成后，哪些事情应该成立？

## 非目标

- 哪些内容明确不在本次范围内？

## 范围

- 可能受影响的文件、模块、用户、API、数据、部署或文档。

## 需要确认的决定

- 实施前需要用户或维护者确认什么？

## 生命周期说明

- 讨论中使用 `draft`。
- 实施中使用 `active`。
- 只有验证完成且稳定结论已同步回当前态文档后，才使用 `done`。
- 只有该 change 已变成历史材料且不应作为活跃上下文时，才使用 `archived`。
