# ForgeKit 模板说明

本文件用于维护模板；fresh init 不安装业务根 README。
0.47 使用七个主要 Skill 和三个显式兼容别名。按 AGENTS 或 CLAUDE 的任务入口读取所需分支。
用户说明见 .forgekit/docs/usage-playbook.md；事实归属见 document-responsibility；验证命令见 testing；保存恢复见 work-session-checkpoint。
Markdown 隐藏分区支持结构化升级。保留用户内容；真实冲突阻止整个应用。模板结构、投影或入口变化后运行 scripts/validate-template.ps1，验证新安装和迁移。
整份重写的 AGENTS / CLAUDE 需要复核并显式提供入口合并材料；流程见 `.forgekit/docs/project-maintenance.md`。
