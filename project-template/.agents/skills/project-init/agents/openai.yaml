interface:
  display_name: "project-init"
  short_description: "project init"
  default_prompt: "$project-init\n\nInitialize ForgeKit through its unified project entry when the user requests installation or initialization."
policy:
  allow_implicit_invocation: true
