interface:
  display_name: "document-backfill"
  short_description: "document backfill"
  default_prompt: "$document-backfill\n\nFill bootstrap gaps or backfill existing owner documents from evidence, only on an explicit request."
policy:
  allow_implicit_invocation: false
