interface:
  display_name: "security-review"
  short_description: "security review"
  default_prompt: "$security-review\n\nReview concrete security boundaries, sensitive data or untrusted inputs. Not a generic quality review."
policy:
  allow_implicit_invocation: true
