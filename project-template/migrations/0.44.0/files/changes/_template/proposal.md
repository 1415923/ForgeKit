Status: draft
Risk: medium
Created: YYYY-MM-DD
Owner: <name>
Reason: <short reason>

# 变更提案

## 问题

- 要解决什么问题？

## 目标

- 变更完成后，哪些事情应该成立？

## 非目标

- 哪些内容明确不在本次范围内？

## 范围

- 可能受影响的文件、模块、用户、API、数据、部署或文档。

## 信任边界

- 本次防止哪些类型的误用、错误执行或错误结果？
- 哪些调用者、内部 helper 或运行环境明确不在防护边界内？

## 阶段授权

- 当前允许到哪一步：`design-ready`、`implementation-ready-for-review`、`ready-for-commit`、`ready-for-minimal-real-smoke` 或 `ready-for-full-execution`？
- 进入下一阶段还需要哪些证据或人工确认？

## 需要确认的决定

- 实施前需要用户或维护者确认什么？

## 冻结规则

- medium/high risk change 在实现前冻结目标、范围、信任边界、非目标和阶段授权；变更这些内容应返回设计阶段并留下原因。
- low risk change 可保持轻量，不要求为了填写模板而扩大流程。

## 生命周期说明

- 讨论中使用 `draft`。
- 实施中使用 `active`。
- 只有验证完成且稳定结论已同步回当前态文档后，才使用 `done`。
- 只有该 change 已变成历史材料且不应作为活跃上下文时，才使用 `archived`。

