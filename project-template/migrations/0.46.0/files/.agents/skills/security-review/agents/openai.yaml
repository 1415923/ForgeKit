interface:
  display_name: "Security Review"
  short_description: "Audit real security boundaries read-only"
  default_prompt: |-
    $security-review
    Review the specified security boundary and report canonical impact severity and Blocking independently, with evidence, scoped failure paths, and verification gaps, without modifying files.
