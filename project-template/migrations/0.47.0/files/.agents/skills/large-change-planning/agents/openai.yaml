interface:
  display_name: "large-change-planning"
  short_description: "large change planning"
  default_prompt: "$large-change-planning\n\nPlan changes with material trust, compatibility, migration or recovery impact. Do not trigger from file count."
policy:
  allow_implicit_invocation: true
