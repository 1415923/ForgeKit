interface:
  display_name: "release-check"
  short_description: "release check"
  default_prompt: "$release-check\n\nAssess an explicit release candidate and its evidence. Does not publish or treat ordinary commits as releases."
policy:
  allow_implicit_invocation: true
