# React 命令示例

```powershell
node --version
npm --version
pnpm --version
npm install
npm run dev
npm run build
npm run lint
npm run typecheck
npm test
```

注意：

- 安装依赖先确认。
- dev server 先确认，必要时用正常本机权限启动。
